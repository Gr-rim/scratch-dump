# OS cruft
.DS_Store
._*
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Packaged builds
*.zip
*.crx
*.pem
web-ext-artifacts/

# Tooling (none committed yet — see audit L11)
node_modules/
npm-debug.log*
.eslintcache

# Local env
.env
.env.local

# Mobile client — its own project, with its own repository. Deployed to
# GitHub Pages as a web app and built separately as an Android app.
scratch-bump/

# Working copies of the mobile client, folded into scratch-bump/ in 1.1.8.
# Listed so a future scratch copy cannot be committed here by accident.
new_changes/
scratch-bump-updated/

# Throwaway network spike — not part of the extension
spike/
